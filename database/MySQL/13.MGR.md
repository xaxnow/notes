###　１.简介
```
一般主从复制概念扩展,多个节点共同组成一个数据库集群，事务的提交必须经过半数以上节点同意方可提交,在集群中每个节点上都维护一个数据库状态机，保证节点间事务的一致性。
数据强一致：即使发生failover，数据也保证不丢、不乱。
服务高可用：自治的探活机制，发生故障自动failover。
Multi-Master：具有冲突检测、解决机制，允许多个Master同时承担写流量。
```